spring.application.name=Ecometa 
 
# ─── MongoDB Configuration ────────────────────────────────────────────────────
# Loads from .env or uses local fallback
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://onkardoke796_db_user:v5dqnNTqh51MpkNw@cluster0.l7encge.mongodb.net/ecometa?retryWrites=true&w=majority&authSource=admin}
spring.data.mongodb.database=ecometa
spring.data.mongodb.auto-index-creation=true

# ─── E-Mail (Gmail SMTP) ──────────────────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_USER:justthinkme143@gmail.com}
spring.mail.password=${GMAIL_PASS:abcd xyza asdf}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
 
# ─── Certificate PDF Storage ──────────────────────────────────────────────────
certificate.directory=./certificates
 
# ─── Security / JWT ───────────────────────────────────────────────────────────
jwt.secret=${JWT_SECRET:U2VjdXJlS2V5V2l0aE1vcmVDaGFyYWN0ZXJzMTIzIQ==}
 
# ─── CORS ─────────────────────────────────────────────────────────────────────
frontend.url=${FRONTEND_URL:http://localhost:3000}

# ─── Server ───────────────────────────────────────────────────────────────────
# Crucial: Default to 8080 if PORT is not set, to avoid NumberFormatException
server.port=${PORT:8080}
