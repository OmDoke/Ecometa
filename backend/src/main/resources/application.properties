spring.application.name=Ecometa 
 
# ─── MongoDB Configuration (replaces MySQL) ───────────────────────────────────
# Set MONGODB_URI as environment variable on Render.com
# Local dev fallback:
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://<user>:<password>@cluster0.l7encge.mongodb.net/ecometa?retryWrites=true&w=majority&authSource=admin}
spring.data.mongodb.database=ecometa

# ─── E-Mail (Gmail SMTP) ──────────────────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_USER:justthinkme143@gmail.com}
spring.mail.password=${GMAIL_PASS:abcd xyza asdf oufsjnvs  sofi sov}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
 
# ─── Certificate PDF Storage ──────────────────────────────────────────────────
certificate.directory=./certificates
 
# ─── Security / JWT ───────────────────────────────────────────────────────────
jwt.secret=${JWT_SECRET:abcd}
 
# ─── CORS ─────────────────────────────────────────────────────────────────────
# Set FRONTEND_URL on Render dashboard to your Vercel URL
frontend.url=${FRONTEND_URL:http://localhost:3000}
 
# ─── Server ───────────────────────────────────────────────────────────────────
server.port=${PORT:8080}
